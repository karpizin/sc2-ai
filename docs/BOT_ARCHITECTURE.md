# Архитектура и функционирование ProtossModularBot

Этот документ подробно описывает внутреннее устройство ИИ-агента для StarCraft II, реализованного на языке Python с использованием библиотеки `python-sc2`.

---

## 1. Концепция: Менеджер-ориентированная архитектура

Бот спроектирован по модульному принципу. Вместо единого цикла принятия решений, логика распределена между независимыми **Менеджерами**, каждый из которых отвечает за свою узкую область. Это позволяет легко масштабировать бота и в будущем заменять отдельные модули (например, на нейросетевые).

Оркестрация осуществляется в главном классе `ProtossModularBot`, который на каждом игровом тике (`on_step`) опрашивает всех менеджеров.

---

## 2. Обзор ключевых модулей

### 🧠 IntelManager (Глобальная память и Аналитика)
Самый важный модуль для принятия стратегических решений.
*   **Память о зданиях**: Бот хранит координаты всех увиденных зданий противника. Если здание уходит в «туман войны», бот помнит его местоположение, пока не разведает это место снова и не убедится, что здания там больше нет.
*   **Детекция стратегий**: Анализирует время игры и увиденное на базе врага.
    *   **PROXY_DETECTED**: Если время > 1:40, база врага разведана, но там нет производящих зданий (гейтов/бараков) — бот делает вывод, что они построены тайно у наших границ.
    *   **RUSH/CHEESE**: Детектируется по аномально низкому количеству рабочих или ранним агрессивным зданиям.
    *   **MACRO**: Если бот видит 2+ ратуши противника в начале игры.

### 💰 EconomyManager (Экономика и Оптимизация)
Отвечает за фундамент — добычу ресурсов.
*   **Smart Gas Management**: Бот строго контролирует, чтобы на каждом ассимиляторе было ровно 3 рабочих. Он динамически перераспределяет пробки между минералами и газом.
*   **Mineral Micro (Ускоренный майнинг)**: Применяет низкоуровневую оптимизацию движения. Вместо стандартной команды «добывать», бот отдает команду «двигаться» на полной скорости почти до самого кристалла и только в последний момент переключает на «добывать». Это убирает фазу торможения юнита, повышая доход на несколько процентов.
*   **Chrono Boost**: Автоматически расходует энергию Нексусов на ускорение производства рабочих и критических технологий.

### 🏗 ProductionManager (Строительство и Экспансия)
Выполняет роль «архитектора» и «директора по развитию».
*   **Адаптивный Build Order**: Бот не следует жесткому списку. Он строит здания исходя из приоритетов: сначала инфраструктура (Gateway, CyberCore), затем масштабирование.
*   **Динамическая Экспансия**: Бот строит 3-ю, 4-ю и последующие базы, когда текущие почти насыщены рабочими или когда накоплен избыток ресурсов (> 800 минералов).
*   **Реактивная Оборона**: Если IntelManager сигнализирует о прокси-угрозе, ProductionManager мгновенно меняет приоритет на постройку **Forge**, **Shield Battery** и **Photon Cannon** для защиты.

### 🔭 ScoutManager (Разведка и Поиск)
Отвечает за сбор информации «в поле».
*   **Probe Scout**: В начале игры (на 13 лимите) выделяет рабочего для объезда стартовых позиций врага. Умеет уклоняться от вражеских юнитов и «кружить» вокруг базы.
*   **Emergency Proxy Search**: Если заподозрена прокси-атака, бот выделяет до 3-х юнитов (приоритет армии, иначе рабочие) и рассылает их по «точкам интереса» вокруг нашей базы, чтобы найти и подсветить скрытые здания врага.

### ⚔️ ArmyManager (Военная логика)
Управляет наймом войск и их поведением на карте.
*   **Production**: Приоритезирует Сталкеров и Адептов. Занимается трансформацией гейтвеев в Варп-гейты.
*   **Defensive Posture**: В случае угрозы стягивает всю армию к главной рампе и защищает рабочих.
*   **Adaptive Attack**: Атакует главную базу врага при достижении определенного лимита (по умолчанию 15 боевых единиц). При активной угрозе порог атаки автоматически повышается, чтобы не оставлять базу без защиты.

---

## 3. Визуализация и Отладка (Debug UI)

Для удобства разработки в бот встроена система визуализации:
*   **Текстовые статусы**: Текущая догадка о стратегии врага и счетчик увиденных рабочих выводятся в верхнем левом углу.
*   **Сферы памяти**: Здания в тумане войны подсвечиваются зелеными сферами.
*   **Маркеры ролей**: Юниты, занятые в поиске прокси, помечаются текстом "PROXY SEARCHER" прямо над их головами в игре.

---
*Этот бот представляет собой гибридную систему, сочетающую жесткие правила микроконтроля с гибким стратегическим анализом.*
